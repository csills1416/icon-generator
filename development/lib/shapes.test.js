const { Triangle, Square, Circle } = require('./shapes');

describe ('Triangle', () => {
    it ('should create a triangle', () => {
        // Write a test for Triangle class
        }));
describe ('Square', () => {
    it ('should create a square', () => {
        // Write a test for Square class
        }));
        
describe ('Circle', () => {
    it ('should create a circle', () => {
        // Write a test for Circle class
        }));

// Note to self: We should go back to this. Need to study more.